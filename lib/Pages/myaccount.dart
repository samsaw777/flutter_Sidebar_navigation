import "package:flutter/material.dart";

class MyAccount extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("MyAccount", style: TextStyle(fontWeight: FontWeight.w900, fontSize: 28)));
  }
}
